services:
  rc-admin:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:8080"
    environment:
      - NODE_ENV=production
      - VITE_API_BASE_URL=http://4.240.119.167
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge